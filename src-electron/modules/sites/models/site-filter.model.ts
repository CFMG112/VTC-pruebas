import { BaseFilter } from '../../../shared/base-filter.model';

export class SiteFilter extends BaseFilter {
  
  name: string;
  country: string;
  state: string;
  city: string;
  address: string;
}

